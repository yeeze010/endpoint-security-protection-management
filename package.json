{
  "name": "endpoint-security-protection-management",
  "version": "0.1.0",
  "private": true,
  "description": "Endpoint security protection and management platform.",
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "npm --prefix frontend run dev",
    "build": "npm run build:web && npm run package:api",
    "build:web": "npm --prefix frontend run build",
    "preview:web": "npm --prefix frontend run preview",
    "typecheck:web": "npm --prefix frontend run typecheck",
    "test": "npm run typecheck:web && npm run test:api",
    "test:api": "scripts\\run-maven.cmd test",
    "package:api": "scripts\\run-maven.cmd package",
    "verify": "npm run build:web && npm run test:api",
    "start:local": "powershell -ExecutionPolicy Bypass -File ./scripts/start-local.ps1"
  }
}
